\section{ѕостановка задачи}

¬о множестве точек на плоскости найти пару точек с максимальным рассто€нием между ними.

\section{јлгоритм решени€ задачи}

1.—оздаем класс точки и пр€мой. ¬ классе точки создаем метод Distanse, который использу€ теорему ѕифагора считает рассто€ние между точками.
2.¬ классе main с помощью двух циклов for перебираем все пары точек, максимальное рассто€ние записываетс€.
3.—оедни€ем точки между которыми максимальное рассто€ние пр€мой дл€ нагл€дности. 

ƒобавили эту строку: 

\subsection{Ѕазовые структуры данных}

\subsection{»сходные данные}

ћножество точек на плоскости.

\subsection{¬ыходные данные}

ћаксимальное рассто€ние между точками.

\section{Ћистинг программы}

{
	for (int i = 0; i < points.size(); i++) {
	for (int j = i + 1; j < points.size(); j++) {							//просчитываем всевозможные вариации с точками
	double d = Point.Distanse(points.get(i), points.get(j));
	if (max < d) {
	max = d;																//если нашли больше, то новый max
	Max1 = i;
	Max2 = j;
	}
	}
	}
	//System.out.println(max);												//вывод max
	
	com.bot.Line l = new com.bot.Line(points.get(Max1), points.get(Max2));  //создаем пр€мую, котора€ будет соедин€ть точки 
	lines.add(l);
	l.setBounds(Max1, Max2, frame.getWidth(), frame.getHeight());
	pointpane.add(l);
	pointpane.revalidate();
	pointpane.repaint();
	answerL.setText("ќтвет: " + max);										//выводим ответ на панель